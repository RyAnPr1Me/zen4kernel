diff --git a/Makefile b/Makefile
index 1234567..abcdefg 100644
--- a/Makefile
+++ b/Makefile
@@ -606,6 +606,11 @@ KBUILD_CFLAGS += -mllvm -polly
 KBUILD_CFLAGS += -mllvm -polly-vectorizer=stripmine
 endif
 
+# SMS (Swing Modulo Scheduling) - Software pipelining for better ILP
+ifdef CONFIG_CC_IS_CLANG
+KBUILD_CFLAGS += -fmodulo-sched -fmodulo-sched-allow-regmoves
+endif
+
 KBUILD_RUSTFLAGS-$(CONFIG_WERROR) += -Dwarnings
 KBUILD_RUSTFLAGS += $(KBUILD_RUSTFLAGS-y)
